$(document).ready(function(){

	//geo code object 
	// need a geocode request object to the server
	//turn geocode into lat and lngi

 /*
	var geocode = google.maps.Geocoder();
	goecoder.geocode({'address':address}, function(results, status){
		if(status == google.maps.GeocoderStatus.OK){


		map.setCenter(results[0].geometry.location);
		var marker = new google.maps.Marker({
			map: map, 
			position: results: results[0].geometry.location, 

		});

		}


	});
	 

*/ //just going to do it server side on python

});
